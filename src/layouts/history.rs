use leptos::{create_resource, create_signal, view, IntoView};

pub fn History() ->impl IntoView {
    let (count, set_count) = create_signal(0);

   

    view! {
        <div>

        </div>
    }
}